TASKS, TO DOS, ETC


write action creators and reducers to handle dispatching updates to the scores in the server
hook up dispatch to SubmitScore component

configure timer state, pass to component: create based on timestamping first play and diffing it with win state


sort scores object in action creator before passing it to state
make ScoresContainer scrollable
break InfoContainer up into smaller components

somehow offload text content to someplace that's not hardcoded to the component for ease up updating